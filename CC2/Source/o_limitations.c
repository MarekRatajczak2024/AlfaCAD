//
// Created by Marek Ratajczak on 05/30/26.
//
#define elmer_limits_en u8"The advanced Plate/Shield structural solver requires a 64-bit operating system architecture to handle heavy mathematical computations and sparse matrix memory allocation. Please run the 64-bit version of AlfaCAD on a compatible modern system to use this feature."


// --- ENGLISH ---
#define _PLATE_NO_32BIT_A_ u8"The plate looks OK, but the Elmer FEM compute module doesn't work in a 32-bit system."
#define _PLATE_NO_32BIT_B_ u8"Buy a new computer."

// --- POLISH (PL) ---
#define _PLATE_NO_32BIT_A_ u8"Płyta wygląda OK, ale moduł obliczeniowy Elmer FEM nie pracuje w systemie 32-bitowym."
#define _PLATE_NO_32BIT_B_ u8"Kup nowy komputer."

// --- UKRAINIAN (UA) --- (Fixed 'Плата' to 'Плита' - 'Плата' means a electronic circuit board, 'Плита' is a structural plate)
#define _PLATE_NO_32BIT_A_ u8"Плита виглядає нормально, але обчислювальний модуль Elmer FEM не працює в 32-бітній системі."
#define _PLATE_NO_32BIT_B_ u8"Купіть новий комп'ютер."

// --- SPANISH (ES) ---
#define _PLATE_NO_32BIT_A_ u8"La losa parece estar bien, pero el módulo de cálculo Elmer FEM no funciona en un sistema de 32 bits."
#define _PLATE_NO_32BIT_B_ u8"Compra una computadora nueva."

// --- ENGLISH ---
#define _SHIELD_NO_32BIT_A_ u8"The shield looks OK, but the Elmer FEM compute module doesn't work in a 32-bit system."
#define _SHIELD_NO_32BIT_B_ u8"Buy a new computer."

// --- POLISH (PL) ---
#define _SHIELD_NO_32BIT_A_ u8"Tarcza wygląda OK, ale moduł obliczeniowy Elmer FEM nie pracuje w systemie 32-bitowym."
#define _SHIELD_NO_32BIT_B_ u8"Kup nowy komputer."

// --- UKRAINIAN (UA) --- (Short, safe, and fits your byte limitations perfectly)
#define _SHIELD_NO_32BIT_A_ u8"Щит виглядає нормально, але обчислювальний модуль Elmer FEM не працює в 32-бітній системі."
#define _SHIELD_NO_32BIT_B_ u8"Купіть новий комп'ютер."

// --- SPANISH (ES) ---
#define _SHIELD_NO_32BIT_A_ u8"La pantalla parece estar bien, pero el módulo de cálculo Elmer FEM no funciona en un sistema de 32 bits."
#define _SHIELD_NO_32BIT_B_ u8"Compra una computadora nueva."